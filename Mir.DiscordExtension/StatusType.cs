namespace Mir.DiscordExtension
{
    public enum StatusType
    {
        PlayerCount,
        GameState,
        Party,
        SmallImageText,
        LargeImageText,
        PlayerName,
        PlayerLevel,
    }
}