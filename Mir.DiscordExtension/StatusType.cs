namespace Mir.DiscordExtension
{
    public enum StatusType
    {
        PlayerCount,
        GameState,
        Party,
        SmallImage,
        SmallImageText,
        LargeImage,
        LargeImageText,
        PlayerName,
        PlayerLevel,
        PlayerClass
    }
}